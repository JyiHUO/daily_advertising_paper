任务：根据一篇论文里的单个技术点，直接生成一张 SVG 技术讲解图。

要求：
- 目标不是复刻论文原图，而是把一个技术点压缩成一张更易懂的“讲解图”。
- 必须同时参考技术细节、通俗讲解和例子，不要只摘抄其中一段。
- 如果论文主图已经体现出清楚的模块关系、信息流或输入输出结构，应尽量沿着那个结构去解释这个技术点。
- 这张图只讲“一个核心知识点”，不要试图覆盖这个技术点的所有细节、所有子模块、所有损失项或所有实验现象。
- 如果一个技术点里包含多个并列设计，只保留那个最能解释“它为什么有效”的主机制。
- 整张图优先让读者扫一眼就懂，不要逼读者读很多字。
- 文字请短，尽量像图上的标签，而不是讲义正文。
- 你可以自由选择表现方式，例如：building block、输入输出流程、前后对比、两三格分镜、小漫画、局部放大、信息流示意。
- 只要能更通俗易懂，不必拘泥于固定版式。
- 如果论文内容很多，请主动压缩、合并、抽象，不要把多个贡献点拆成很多点堆上去。
- 如果你发现自己想写很多字，说明方向错了：请继续压缩，直到它像一张可以贴在白板上的示意图。
- 避免使用“因为…所以…并且…”这种长句，优先使用短词组、短动宾结构、短标签。
- 不要输出 JSON，不要输出 markdown 代码块，不要输出解释文字，只输出一个完整 SVG 文档。
- 语言要通俗，不要堆术语，不要把每个字段都写得像论文原文。
- 不要输出公式、LaTeX、复杂数学符号、代码片段。
- 不要编造论文里没有的模块名；如果信息不足，就用更抽象但仍准确的说法。
- SVG 必须自包含，可直接渲染；不要引用外部资源，不要使用脚本，不要使用 foreignObject。
- SVG 里可以包含 `<title>` 或 `<desc>`，用一句短话说明图在讲什么。

论文标题：
UniMixer: A Unified Architecture for Scaling Laws in Recommendation Systems

论文摘要背景：
{
  "background": "推荐/广告排序模型近年来借鉴LLM的Scaling Law思路，试图通过堆叠更多层、更大参数来持续提升AUC。目前业界主流有三类scaling模块：基于异构Attention的(如HiFormer、FAT)、基于TokenMixer的(如RankMixer)、基于FM的(如Wukong)，三者设计哲学和结构差异很大，缺乏统一认识，导致研究者难以判断哪种结构scaling效率最优、也无法同时利用各方优势。UniMixer由快手广告团队提出，旨在建立一个统一理论框架将三类方法连接起来，并设计出兼具三方优点、参数效率和计算效率都更优的轻量模块UniMixing-Lite，在快手广告用户留存预估场景上线后30天累计活跃天数(CAD)平均提升超过15%。",
  "pick_reason": "快手广告团队提出的推荐系统统一Scaling架构UniMixer，将Attention、TokenMixer、FM三大主流排序模型scaling模块统一到一个理论框架下，并在快手广告投放场景(用户留存预估)取得了显著的离线和在线收益。该工作直接应用于广告排序模型，对广告CTR/CVR预估模型的架构设计和参数效率优化有极强的参考价值。"
}

已选主图信息：
{
  "caption": "Figure 2: The UniMixer architecture for scaling laws in recommendation systems.",
  "selection_reason": "该图是完整且聚焦的主架构图，标题直接表明为“UniMixer architecture”，清楚展示了Embedding Layer、Token-Specific Linear Layer、UniMixer Block、UniMixing/PerToken SwiGLU、任务塔以及左右分支（UniMixing 与 UniMixing-Lite）之间的模块关系与信息流，最能代表论文核心方法。相比之下，Figure 1 和 Figure 4 都是 scaling law 结果曲线，不是方法总览；page-4-block-29 虽然也是同一图，但包含较多正文噪声，不如当前这个裁剪干净。",
  "kind": "drawing_cluster_with_caption"
}

技术点输入：
{
  "title": "温度退火与稀疏性训练策略",
  "technical_detail": "全局矩阵W-G和局部矩阵W-B通过Sinkhorn-Knopp迭代投影到双随机矩阵空间，投影前先除以温度系数tau。tau小则矩阵趋近于稀疏的置换矩阵(每行每列几乎只有一个非零元素)，tau大则矩阵接近均匀分布。训练时采用线性退火：tau从1.0线性降至0.05。当数据不足时，还可以先用高温训练到收敛，再用低温从高温模型权重初始化继续训练(类似warm-up)。消融实验显示去掉温度系数AUC下降0.16%，去掉warm-up下降0.086%，是影响最大的两个因素。",
  "plain_explanation": "稀疏性对模型效果至关重要——让每个block只跟少数其他block强交互，避免信息被平均化。但一开始就很稀疏会导致梯度不稳定。所以训练初期用高温让矩阵比较平滑、梯度好传播，随着训练推进逐渐降温让矩阵变稀疏。这就像先用软笔画草图找到大致方向，再用硬笔精确勾勒细节。",
  "example": "训练初期tau=1.0时，128x128的全局矩阵每个元素大约是1/128=0.0078，信息几乎均匀流动。训练后期tau降到0.05时，矩阵变得非常尖锐，大部分元素接近0，每行只有1-2个位置有较大的值，形成了类似置换矩阵的稀疏模式。从可视化图中可以看到低温下的矩阵呈现出清晰的条纹状稀疏结构。"
}

输出要求：
输出要求：
- 只输出一个完整的 SVG 文档，直接从 <svg ...> 开始，到 </svg> 结束，不要输出 JSON，不要输出 markdown 代码块，不要输出解释。
- 这张图的目标是让读者一眼看懂一个技术创新点，版式由你自己决定，不要被固定模板限制。
- 文字尽量少，优先靠图形、箭头、布局、对比、示意元素表达。
- 只讲 1 个最重要创新点，不要把多个贡献全塞进去。
- 可以参考论文主图的模块关系，但不要机械复刻。
- SVG 必须自包含，可直接渲染：不要引用外部资源，不要使用脚本，不要使用 foreignObject。
- 默认白底，宽度建议 1200-1600，高度建议 800-1100。
- 请克制复杂度，避免密集网格、海量重复小方块或过度细节；优先用少量大模块讲清楚。
- 文本请使用中文短标签，避免大段说明。